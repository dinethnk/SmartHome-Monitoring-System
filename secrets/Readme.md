# Firebase Admin Credentials

This folder is used to store the Firebase Admin SDK service account credentials required by the backend functions.

## Setup

Before running the backend functions, add your Firebase Admin SDK credential JSON file to this directory:

```text
secrets/
└── <firebase-admin-credential>.json