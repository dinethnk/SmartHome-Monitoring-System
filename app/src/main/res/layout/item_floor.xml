<?xml version="1.0" encoding="utf-8"?>

<com.google.android.material.card.MaterialCardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/floorCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/spacing_16"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackground"
        app:cardBackgroundColor="@color/surface"
        app:cardCornerRadius="@dimen/radius_medium"
        app:cardElevation="2dp"
        app:strokeWidth="0dp">

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="110dp"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:padding="@dimen/spacing_16">

        <!-- Floor icon -->

        <ImageView
                android:id="@+id/imageFloor"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:background="@drawable/bg_stat_icon"
                android:backgroundTint="@color/primary_container"
                android:contentDescription="Floor icon"
                android:padding="@dimen/spacing_12"
                android:src="@drawable/ic_floor"
                app:tint="@color/primary" />


        <!-- Floor information -->

        <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/spacing_16"
                android:layout_weight="1"
                android:orientation="vertical">

            <TextView
                    android:id="@+id/textFloorItemName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textAppearance="?attr/textAppearanceTitleMedium"
                    tools:text="Ground Floor" />

            <TextView
                    android:id="@+id/textFloorItemDeviceCount"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_4"
                    android:textAppearance="?attr/textAppearanceBodySmall"
                    tools:text="3 connected devices" />

            <TextView
                    android:id="@+id/textFloorItemStatus"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_8"
                    android:background="@drawable/bg_status_chip"
                    android:backgroundTint="@color/primary_container"
                    android:paddingHorizontal="@dimen/spacing_12"
                    android:paddingVertical="@dimen/spacing_4"
                    android:textColor="@color/active"
                    android:textAppearance="?attr/textAppearanceLabelSmall"
                    android:textStyle="bold"
                    tools:text="ACTIVE" />

        </LinearLayout>


        <!-- Floor actions -->

        <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">



            <!-- Edit floor -->

            <ImageButton
                    android:id="@+id/buttonEditFloor"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Edit floor"
                    android:padding="@dimen/spacing_8"
                    android:src="@drawable/ic_edit"
                    app:tint="@color/primary" />

            <!-- Delete floor -->

            <ImageButton
                    android:id="@+id/buttonDeleteFloor"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Delete floor"
                    android:padding="@dimen/spacing_8"
                    android:src="@drawable/ic_delete"
                    app:tint="@color/error" />

            <!-- Open floor -->

            <ImageButton
                    android:id="@+id/buttonOpenFloor"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Open floor"
                    android:padding="@dimen/spacing_8"
                    android:src="@drawable/ic_arrow_right"
                    app:tint="@color/primary" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>