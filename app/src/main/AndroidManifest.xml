<?xml version="1.0" encoding="utf-8"?>

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission
        android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartHomeMonitoringSystem"
        android:label="@string/app_name">

        <activity android:name=".view.reports.ReportsActivity" />
        <activity android:name=".view.camera.CameraActivity" />
        <activity android:name=".view.schedule.LightScheduleActivity" />

        <activity android:name=".view.devices.SafetyDeviceActivity" />
        <activity android:name=".view.devices.MultiSwitchControlActivity" />
        <activity android:name=".view.devices.OutletControlActivity" />

        <activity android:name=".view.floors.FloorPlanActivity" />
        <activity android:name=".view.floors.FloorFormActivity" />
        <activity android:name=".view.floors.ManageFloorsActivity" />

        <activity
            android:name=".view.dashboard.DashboardActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>